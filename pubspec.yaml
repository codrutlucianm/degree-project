name: parkinson_detection_app
description: Bachelor's Degree Project

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animated_splash_screen: ^1.0.1+2
  google_fonts: any
  shared_preferences: ^0.5.12+4
  cupertino_icons: ^1.0.0
  sqflite: ^2.0.0+3
  provider: ^4.3.2+3
  sensors: any
  curved_navigation_bar: ^0.3.7
  foreground_service: ^2.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/Logo.png
    - assets/axis_dark.jpg
    - assets/axis_light.png

